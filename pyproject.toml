[project]
name = "hackathon"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "lyricsgenius",
    "deepl",
    "python-dotenv",
    "pykakasi",
    "korean-romanizer",
]

[project.scripts]
lyrics-pipeline = "pipeline:main"
